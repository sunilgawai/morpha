# apps/

Deployable applications built on the engine (future): a development
playground and a documentation site are the expected first residents.
Empty by design until the packages they would consume exist — placeholder
apps would violate Design Principle 7 (earn abstractions).
