# tests/regression/

**Regression Tests**

One test per fixed cross-package bug, named for its issue and kept forever, so a resolved defect can never silently return. Single-package regressions live with their package.

See [../README.md](../README.md) for the full taxonomy and the single-
vs-cross-package dividing line.
