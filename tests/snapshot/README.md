# tests/snapshot/

**Cross-package Snapshots**

Structural snapshots that cross package boundaries — SSR HTML output (renderer-ssr) and `RenderState` shape. Per-widget/component snapshots belong in the owning package's `test/`.

See [../README.md](../README.md) for the full taxonomy and the single-
vs-cross-package dividing line.
