# tools/

Repo-local tooling packages (custom lint rules, codegen, release tooling)
that are part of the workspace but never published. Empty until a real
tool is needed; one-off scripts belong in scripts/ instead.
