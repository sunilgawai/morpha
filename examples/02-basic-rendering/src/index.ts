// Example 02 — Basic Rendering. Structure only (ADR-0011).
// Project a document to a RenderState and render it once — renderers are stateless projections that never mutate.
// Implementation begins once the packages it teaches exist.
export {};
