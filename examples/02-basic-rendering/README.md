# Example 02 — Basic Rendering

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Project a document to a RenderState and render it once — renderers are stateless projections that never mutate.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on rendering + a renderer package.

Relevant reading path (Architecture-Index.md §9): Renderer developers
