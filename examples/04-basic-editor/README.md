# Example 04 — Basic Editor

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** A minimal interactive editor (select, move) demonstrating the closed mutation loop — input becomes intent becomes Command.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on interaction + renderer-dom + react.

Relevant reading path (Architecture-Index.md §9): New contributors, then Renderer developers
