# Example 13 — React Integration

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Mount an editor with @morpha/react hooks — session-scoped bindings (ADR-0004).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on react + renderer-dom.

Relevant reading path (Architecture-Index.md §9): Renderer developers
