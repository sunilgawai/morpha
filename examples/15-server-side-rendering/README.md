# Example 15 — Server-side Rendering

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Render a document to HTML on the server — the SSR renderer relies on the deterministic core (Principle 8).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on renderer-ssr.

Relevant reading path (Architecture-Index.md §9): Renderer developers
