// Example 15 — Server-side Rendering. Structure only (ADR-0011).
// Render a document to HTML on the server — the SSR renderer relies on the deterministic core (Principle 8).
// Implementation begins once the packages it teaches exist.
export {};
