{
  "name": "example-15-server-side-rendering",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Example 15 — Server-side Rendering (structure only). Render a document to HTML on the server — the SSR renderer relies on the deterministic core (Principle 8)."
}
