# Example 16 — AI Integration

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Drive the engine from an AI provider that emits Commands — AI is a Command-producing adapter, not a privileged path.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on AI.md (deferred); seam fixed by Plugin-System.md §9.5.

Relevant reading path (Architecture-Index.md §9): AI integration developers
