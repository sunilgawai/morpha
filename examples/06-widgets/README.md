# Example 06 — Widgets

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Compose a document from built-in widgets (rect, group, image) resolved through the Widget Registry.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on widgets-base.

Relevant reading path (Architecture-Index.md §9): Plugin developers
