// Example 06 — Widgets. Structure only (ADR-0011).
// Compose a document from built-in widgets (rect, group, image) resolved through the Widget Registry.
// Implementation begins once the packages it teaches exist.
export {};
