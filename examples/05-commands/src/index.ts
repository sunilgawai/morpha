// Example 05 — Commands. Structure only (ADR-0011).
// Dispatch commands and undo/redo — Commands are the only mutation mechanism (ADR-0007).
// Implementation begins once the packages it teaches exist.
export {};
