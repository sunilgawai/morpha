# Example 05 — Commands

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Dispatch commands and undo/redo — Commands are the only mutation mechanism (ADR-0007).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on commands + runtime.

Relevant reading path (Architecture-Index.md §9): Interaction / tools developers
