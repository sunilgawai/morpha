// Example 10 — Serialization. Structure only (ADR-0011).
// Save a document, load it back, and run a schema migration — canonical persistence and versioning from day one.
// Implementation begins once the packages it teaches exist.
export {};
