# Example 10 — Serialization

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Save a document, load it back, and run a schema migration — canonical persistence and versioning from day one.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on serialization.

Relevant reading path (Architecture-Index.md §9): Import/Export developers
