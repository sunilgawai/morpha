# Example 03 — Viewer

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** A read-only viewer over a document via the DocumentQuery read surface — no editing, no dispatch.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on runtime + renderer-dom.

Relevant reading path (Architecture-Index.md §9): Renderer developers
