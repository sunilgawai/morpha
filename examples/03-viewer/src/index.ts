// Example 03 — Viewer. Structure only (ADR-0011).
// A read-only viewer over a document via the DocumentQuery read surface — no editing, no dispatch.
// Implementation begins once the packages it teaches exist.
export {};
