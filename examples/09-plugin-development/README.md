# Example 09 — Plugin Development

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Build a plugin: manifest, lazy activation, and extension points — no privileged path over third parties.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on plugin-api.

Relevant reading path (Architecture-Index.md §9): Plugin developers
