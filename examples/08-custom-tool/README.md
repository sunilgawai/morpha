# Example 08 — Custom Tool

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Build an interaction tool as a state machine plus an Intent Interpreter — the only intent-to-Command path (ADR-0002).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on interaction.

Relevant reading path (Architecture-Index.md §9): Interaction / tools developers
