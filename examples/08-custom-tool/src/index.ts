// Example 08 — Custom Tool. Structure only (ADR-0011).
// Build an interaction tool as a state machine plus an Intent Interpreter — the only intent-to-Command path (ADR-0002).
// Implementation begins once the packages it teaches exist.
export {};
