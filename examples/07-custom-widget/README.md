# Example 07 — Custom Widget

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Author and register a widget via the WidgetDefinition contract — first-party and third-party use the identical API (Principle 6).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on widget-api.

Relevant reading path (Architecture-Index.md §9): Plugin developers
