// Example placeholder: a minimal interactive editor built on presentation-runtime + presentation-renderer-dom + presentation-react
// Implementation begins once the packages it depends on exist.
export {};
