# Example 12 — PPTX Import

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Import a PPTX into a document — an importer adapter maps shapes to widgets at the boundary.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on import-pptx and Import-Export.md.

Relevant reading path (Architecture-Index.md §9): Import/Export developers
