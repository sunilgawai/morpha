// Example 12 — PPTX Import. Structure only (ADR-0011).
// Import a PPTX into a document — an importer adapter maps shapes to widgets at the boundary.
// Implementation begins once the packages it teaches exist.
export {};
