# Example 14 — Next.js Integration

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Use the engine inside a Next.js app — server render then hydrate, engine untouched by the framework.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on react + renderer-ssr (external: Next.js).

Relevant reading path (Architecture-Index.md §9): Renderer developers
