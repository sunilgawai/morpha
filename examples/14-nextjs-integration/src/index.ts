// Example 14 — Next.js Integration. Structure only (ADR-0011).
// Use the engine inside a Next.js app — server render then hydrate, engine untouched by the framework.
// Implementation begins once the packages it teaches exist.
export {};
