# Example 01 — Hello Presentation

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Construct an empty PresentationDocument and read it back — the domain model is the single source of truth.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on Phase 1 (presentation-domain).

Relevant reading path (Architecture-Index.md §9): New contributors
