// Example 01 — Hello Presentation. Structure only (ADR-0011).
// Construct an empty PresentationDocument and read it back — the domain model is the single source of truth.
// Implementation begins once the packages it teaches exist.
export {};
