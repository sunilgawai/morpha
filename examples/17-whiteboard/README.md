# Example 17 — Whiteboard

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Build a free-canvas whiteboard on the same core — the engine is agnostic to document shape.

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on interaction + widgets-base.

Relevant reading path (Architecture-Index.md §9): New contributors
