# Example 11 — PPTX Export

**Status: structure only — no implementation yet (ADR-0011).**

**Teaches exactly one concept:** Export a document to PPTX — an exporter adapter converts engine units to EMU at the boundary (Principle 5).

An example is a reference application, not a test and not a demo. It will be
implemented once its packages exist — currently blocked on export-pptx and Import-Export.md.

Relevant reading path (Architecture-Index.md §9): Import/Export developers
