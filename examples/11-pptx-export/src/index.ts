// Example 11 — PPTX Export. Structure only (ADR-0011).
// Export a document to PPTX — an exporter adapter converts engine units to EMU at the boundary (Principle 5).
// Implementation begins once the packages it teaches exist.
export {};
